/** @type {import('next').NextConfig} */
const nextConfig = {
    // assetPrefix: '',
    basePath: '',
    images: {
        domains: [
            "utfs.io"
        ]
    }
};

export default nextConfig;
